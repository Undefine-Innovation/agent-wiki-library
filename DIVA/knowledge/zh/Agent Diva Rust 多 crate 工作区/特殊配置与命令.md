根目录 justfile 提供统一开发入口：`just build` / `just test` / `just check` / `just run -- <args>` / `just e2e-test` / `just ci`；Windows 下通过 `make-diva` 调用 scripts/make-diva.ps1 启动 GUI 及内嵌 Gateway；CI 门禁由 `just e7-automated-release-gate` 串联 feature-gate、laputa-clean-break、cognitive-clean-break、bml-boundary、GUI 自动化检查。