测试通过 `include_str!("../tests/fixtures/g0_runtime_contract.json")` 加载契约 fixture，并用 `tempfile::TempDir` 构造隔离的 ConfigLoader 与 AppState 进行端到端路由断言；上传接口默认限制 50MB (`DefaultBodyLimit::max(50 * 1024 * 1024)`)。