Rust 2021 (rust-version 1.80.0)，使用 `async-trait` 定义异步 `Tool`/`Module` 接口，`tokio::sync::RwLock` 提供并发安全，`serde_json::Value` 承载 JSON Schema，`tracing` 记录日志，`thiserror` 构造错误类型，并通过 `agent_diva_core::audit` 在工具执行全链路 emit `AuditEvent`。