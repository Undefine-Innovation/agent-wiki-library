Rust + Tokio 异步运行时；LLM 交互通过 `agent_diva_providers::LLMProvider` 抽象；工具注册基于 `agent_diva_tooling::ToolRegistry` 与 `ToolSchemaPartition::Deferred` 延迟激活；子代理并发由 `tokio::task::JoinSet` 限流；技能文档解析使用 `serde_yaml`；工作区指令摘要使用 `sha2::Sha256`。