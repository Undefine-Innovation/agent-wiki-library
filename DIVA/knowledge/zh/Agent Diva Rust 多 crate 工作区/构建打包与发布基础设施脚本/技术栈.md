Rust Cargo workspace + Tauri (PNPM) 桌面打包；macOS 使用 `create-dmg`、`lipo` 生成通用二进制；Windows 使用 NSIS + MSI 安装器；Linux/macOS 使用 systemd/launchd 服务管理；模型清单更新使用 Node.js (`js-yaml`、`axios`) 与 Python (`urllib`) 分别对接 OpenRouter/OpenAI 等 OpenAI 兼容 API。