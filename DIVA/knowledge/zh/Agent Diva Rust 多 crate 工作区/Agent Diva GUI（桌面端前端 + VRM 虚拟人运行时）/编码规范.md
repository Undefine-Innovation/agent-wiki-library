- 每个业务域（chat/console/memory/planning/settings/features/diva-mate）采用 components/services/utils/composables 分层目录组织，测试文件与源码同名 .test.ts 并列放置。
- 跨进程/跨包通信统一通过 shared-avatar-protocol 中定义的 commands/events/types 类型声明，avatar-runtime-vrm 与上层 Mate 逻辑均从该包导入而非重复定义。
- 所有可复用的 UI 以 Vue SFC（.vue）形式实现，并通过 composables（useMask/useTheme/useWorkspaceContext 等）抽取响应式状态逻辑。
- API 调用集中在 src/api/*.ts，通过 Tauri invoke 与 Rust 侧命令对应，测试用例与实现同文件命名（如 approvals.test.ts）。
- 国际化文案统一放在 src/locales/{en,zh}.ts，通过 i18n.ts 提供的 t() 函数在组件中引用。