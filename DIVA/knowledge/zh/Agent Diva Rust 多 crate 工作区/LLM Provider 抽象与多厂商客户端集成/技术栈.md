Rust (edition 2021, rust-version 1.80)；HTTP 使用 reqwest + tokio-tungstenite（SSE/WebSocket 流式）；序列化 serde/serde_json/serde_yaml；异步 via tokio + async-trait + futures；重试使用 rand 加抖动；测试依赖 mockito 与 tokio-test。