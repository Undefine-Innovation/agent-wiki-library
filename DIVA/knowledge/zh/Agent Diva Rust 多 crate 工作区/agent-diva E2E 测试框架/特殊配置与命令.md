运行前需设置 `DEEPSEEK_API_KEY` 或 `E2E_API_KEY` 环境变量，否则 `E2EConfig::from_env()` 会返回错误并跳过测试；可通过 `E2E_API_BASE`、`E2E_MODEL`、`E2E_TIMEOUT`、`E2E_MAX_TIMEOUT`、`E2E_TRACE_DIR`、`E2E_SCENARIOS_DIR`、`E2E_JUDGE_MODEL`、`E2E_PROVIDER_NAME` 覆盖默认行为。