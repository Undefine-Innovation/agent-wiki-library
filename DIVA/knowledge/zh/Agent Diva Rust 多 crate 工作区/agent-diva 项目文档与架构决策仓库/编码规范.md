- 每个迭代 slice 使用 `YYYY-MM-DD-<epic>/vX.Y.Z-<slice>` 目录结构，并以 summary/release/acceptance/verification 四个固定文件描述该版本。
- 架构与决策文档顶部标注更新日期与状态（如 D0–D4 批准状态），并通过表格统一呈现领域、依据文件与状态。
- 过期内容不删除而是移入 `dev/archive(old-docs-dont-read-me)/` 或 `research/historical/`，并在当前入口用指针替代原文。
- 研究文档按 R0–R4 编号组织，目标架构（D0–D4）与事实盘点（R0–R4）严格分离，避免将研究建议误作已批准架构。
- 对不再作为当前依据的内容（旧 Persona JSON、memory_md、AutoDream→Governance 链路等）在入口 README 中显式列出禁止项。