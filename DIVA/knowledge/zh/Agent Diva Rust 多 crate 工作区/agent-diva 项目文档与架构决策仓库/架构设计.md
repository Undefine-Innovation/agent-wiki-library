文档按职责分层组织：`architecture/` 仅保留当前可实施依据（`laputa/architecture.md` 为总入口，`current/` 存放运行时边界合同如 prompt-contract、runtime-approval-boundary）；`decisions/` 收录仍有方向性价值的决策摘要；`research/` 按主题分目录存放 R0–R4 研究与历史论文，`historical/` 归档旧调研；`logs/` 以 `YYYY-MM-DD-<epic>/vX.Y.Z-<slice>` 形式记录每次迭代的 summary/release/acceptance/verification 四件套，构成不可篡改的验收证据链；`engineering/` 仅含 CONTRIBUTING、project-context、CHANGELOG 等工程参考；`resources/` 放图片等非文档资源；`dev/archive(old-docs-dont-read-me)/` 隔离过期架构、PRD、Sprint 与旧开发包，明确禁止从当前入口直接引用。根 `docs/README.md` 规定阅读顺序与归档规则，`architecture/README.md` 维护权威决策状态表并声明不得再作为当前依据的内容，形成“当前依据 / 研究证据 / 历史归档”三层边界。