通过 Cargo features 裁剪编译：默认启用 `manager`+`platform`，`orchestrator` 依赖 `manager`+`approval`+`guardian`，`platform` 同时开启三平台目标；可通过环境变量 `AGENT_DIVA_SANDBOX_DISABLED=1|true` 完全禁用沙箱；测试使用 `cargo test -p agent-diva-sandbox`，开发依赖包含 tempfile、tokio-test、sqlx。