需从 species 源码树根目录执行，且机器上必须存在 Go 工具链（`$GOROOT/bin/go`）；常用命令：`go build -o vivy-sdk.exe ./sdk`，随后 `vivy-sdk verify <plugin-dir>`、`vivy-sdk pack --with <plugin> [--out dir]`、`vivy-sdk inspect-artifact <dir>`。