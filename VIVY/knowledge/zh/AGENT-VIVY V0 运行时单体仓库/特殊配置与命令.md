通过 justfile 统一编排：just setup/go mod download、just build/test/ci、just run（:8787 健康端点）、just dev（后端 :8787 + Vite :3015 双进程开发）、just build-split（headless 后端 + 静态 UI）、just docker-up（单容器 SQLite 模式）与 just docker-up-postgres（Postgres 覆盖）。Docker 镜像仅暴露 127.0.0.1:8787，数据持久化在命名卷 vivy-data。