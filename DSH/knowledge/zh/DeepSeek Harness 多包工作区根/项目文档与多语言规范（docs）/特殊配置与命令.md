本地维护双语文档需运行 `pnpm run verify-translation-pairing`（CI 中由 `doc-sync` 触发）检查配对完整性与一致性；编辑任意一侧后需在同一 PR 中更新对端并用 `pnpm run verify-translation-pairing --write <pair>` 重新记录 blob hash；可通过 `--list` 查看当前配对状态，或通过 `resolve-translation-pairing-conflicts` 处理合并冲突。