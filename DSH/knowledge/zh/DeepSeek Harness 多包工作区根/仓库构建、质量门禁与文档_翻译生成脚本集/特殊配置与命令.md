所有 gate 必须通过 `pnpm run <script>` 启动，以便注入 `npm_execpath`；本地全量门禁通过 `pnpm run check-all` 触发，CI 各阶段通过 `node scripts/run-gates.ts <mode>` 选择模式；覆盖率拆分受 `DSH_COVERAGE_PARTITIONS`、并发受 `DSH_GATE_CONCURRENCY`、Web 快照并发受 `DSH_WEB_SNAPSHOT_WORKERS` 控制；构建 profile 通过 `DSH_BUILD_CLIENT_PROFILE` 或 `--profile` 指定。