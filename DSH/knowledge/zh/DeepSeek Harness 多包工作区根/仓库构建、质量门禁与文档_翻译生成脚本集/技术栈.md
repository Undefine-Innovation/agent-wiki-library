Node.js ESM + TypeScript；使用 Vitest 运行 `.spec.ts` 单测；通过 `typescript` API 直接加载 tsconfig 项目图进行语义分析；Markdown 处理使用 `mdast-util-from-mardown` + `micromark-extension-gfm`；子进程统一经 `pnpm-execpath` 解析为 shell-free 调用。