开发需 `corepack enable && pnpm install`，然后 `pnpm build:ts` 编译入口包、`pnpm build:native` 在 Linux 上用 `musl-tools` 提供的 `musl-gcc` 编译本机二进制；测试通过 `pnpm test` 或单独 `pnpm test:entry` / `pnpm test:launcher` 运行，其中 `launcher.test.js` 在非 Linux 环境静默跳过，在 Landlock 不可用时可设 `NALR_REQUIRE_LANDLOCK=1` 强制失败。