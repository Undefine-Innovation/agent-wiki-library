本地开发需先运行 `scripts/build-exe-for-python-sdk.ts` 生成 `runtime/deepseek-harness-sdk-runtime-*` 可执行及其 ripgrep/macOS helper 侧车，否则 `HarnessClient.start()` 会抛出 `FileNotFoundError`；也可设置 `DSH_RUNTIME_MODE=node` 走源码 node 闭包模式（要求系统 PATH 上有 Node ≥22.19）。测试通过 `pytest tests` 运行，SDK 与 runtime 均以 hatchling 构建。