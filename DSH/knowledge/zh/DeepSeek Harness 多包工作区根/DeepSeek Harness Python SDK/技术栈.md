Python ≥3.10，Pydantic v2 做 JSON-RPC 响应模型校验；通过 `subprocess` 以 JSON-RPC 2.0 over stdio 与外部 dsh 进程通信；打包使用 Hatchling（`pyproject.toml`），并通过 `uv.lock` 锁定依赖；运行时产物由 TypeScript 构建脚本 `scripts/build-exe-for-python-sdk.ts` 生成单文件 Node 可执行。