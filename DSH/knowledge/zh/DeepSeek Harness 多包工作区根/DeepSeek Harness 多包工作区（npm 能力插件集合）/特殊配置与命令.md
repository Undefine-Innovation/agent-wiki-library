依赖图通过 `pnpm run gen-module-graph` 生成并在 CI 中 freshness-gated；新增包需遵守 `verify-package-readme-model-experience.ts`、`verify-package-readme-limitations.ts`、`verify-subsystem-pages.ts` 等脚本校验；每个包需自带 `./invariant` 注册运行时契约并由 `verify-package-invariants` 检查。