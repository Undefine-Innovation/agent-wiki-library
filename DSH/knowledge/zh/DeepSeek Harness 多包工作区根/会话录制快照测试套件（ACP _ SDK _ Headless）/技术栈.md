Vitest + Node.js ESM；通过 `@deepseek-ai/dsh-session-snapshot` 提供 fixture 归一化与 tokenization；SDK 路径使用 `@deepseek-ai/dsh-sdk-client` 的 `DeepSeekHarness`；headless 路径复用 `@deepseek-ai/dsh-loader-smoke` 的 `runLoaderSmoke`；模型请求经由 `@deepseek-ai/dsh-llm-replay` 从 `session.jsonl` 回放。